# Prywatne dane urządzenia — NIE publikuj!
ev_charger_secrets.json

# Prywatny kontekst projektu dla Claude Code
CLAUDE.md

# Dane persystentne
ev_charger_data.json

# Pliki systemowe
.DS_Store
Thumbs.db

# Lokalne ustawienia Claude Code (per-maszyna)
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# Manuały sprzętowe — lokalne kopie do podręcznej dokumentacji (copyright vendora,
# nie publikujemy w publicznym repo). Jeśli kiedyś będziesz chciał zacommitować
# konkretny PDF (np. własną dokumentację), użyj `git add -f`.
docs/*.pdf

# Artefakty Playwright MCP — snapshoty drzewa dostępności i zrzuty ekranu
# z weryfikacji w przeglądarce. Generowane przy każdej sesji, bez wartości w repo.
# UWAGA: zrzuty zapisywane pod względną nazwą lądują w korzeniu repo, nie tutaj —
# podawać ścieżkę `.playwright-mcp/nazwa.png`, żeby nie zaśmiecać `git status`.
.playwright-mcp/
